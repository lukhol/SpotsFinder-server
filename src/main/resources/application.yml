spring.datasource:
    username: pbluser
    password: pblpassword
    url: jdbc:mysql://localhost:3306/spots_finder1?useUnicode=yes&characterEncoding=UTF-8
spring.jpa.hibernate.ddl-auto: update
spring:
  jpa:
    show-sql: true
  http:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB
      enabled: true